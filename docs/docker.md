# Docker Guide

## What it does
Docker provides the local runtime environment for Spark, PostgreSQL, and Airflow.

## Common commands
```bash
docker compose up -d --build
```

```bash
docker compose down
```

```bash
docker exec -it ETL_Spark /bin/bash
```